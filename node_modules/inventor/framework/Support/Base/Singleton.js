/**
 * 单例基类
 *
 * @author : sunkeysun
 */

import IClass from './IClass'

export default class Singleton extends IClass {
    static _instance = null

    static getInstance(...args) {
        if (!this._instance) {
            this._insatnce = new this(...args)
        }

        return this._instance
    }
}
